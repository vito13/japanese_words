{
    
    "_使用jp": "SELECT * FROM jpwords where lesson = 19",
    "_使用newword": "SELECT wordid, kana, kanji, roma, chinese, wordtype, lesson FROM jpwords, newwords where newwords.wordid = jpwords.id",
    "_结果随机": "ORDER BY RANDOM()",
    "_限定结果数量": "LIMIT 1",

    
    "all": "SELECT * FROM jpwords",
    "19": "SELECT * FROM jpwords where lesson = 19",
    "20": "SELECT * FROM jpwords where lesson = 20",
    "new": "SELECT wordid, kana, kanji, roma, chinese, wordtype, lesson FROM jpwords, newwords where newwords.wordid = jpwords.id"
}